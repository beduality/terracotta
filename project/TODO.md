# TODO

- Improve project/, move to its own branch and worktree it; update CONTRIBUING.md; inform contributors should first open PR to this branch to get a task to work and avoid rework; contribuing starts here (you can help manage project by writing PRs!)
- Dry-run release
- Update README with installation; improve it in general

- [cli] Add `destroy` command
- Add YAML schema for IDEs
- [cli] Add "create" command
- [cli] Add "import" command
- Abstract state source
- Save state
- Add `overrides`/`providers` config option, so we can change config based on provider (very useful for different ids)
- Add GHA integration

- Add support for:
    - Gallery assets
    - License URL

- Add support for multiple Terracotta files
    - Import mode
    - `platform/` convention

- Add term color library
